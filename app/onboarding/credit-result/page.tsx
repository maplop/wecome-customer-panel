'use client'

import CreditResult from '@/components/Onboarding/CreditResult'

export default function CreditResultPage() {
  return (
    <CreditResult
      salary={3500}
      onNext={() => { }}
      onBack={() => { }}
    />
  )
}
